
import { LocalState } from './client_types';

const localState: LocalState = {
    gameId: null,
    clientId: null,
    playerColor: null,
    playerName: null,
    vp: 0,
}

export default localState;